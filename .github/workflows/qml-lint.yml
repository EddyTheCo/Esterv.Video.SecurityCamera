name: qmllintbot
run-name: Linting QML for PR
on:

  workflow_run:
    workflows: [push-build-release]
    types:
      - completed
jobs:
  checkout-format-pr:
    uses: EddyTheCo/Common/.github/workflows/qml-lint.yml@develop
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      actions: read
      pull-requests: write
